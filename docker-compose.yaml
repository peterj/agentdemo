# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

services:
  agentdemo:
    image: pj3677/agentdemo:0.0.1
    build:
      context: .
      dockerfile: Dockerfile
    command: ["agentdemo", "--local"]
    ports:
      - "8080:8080"
    environment:
      # TODO: remove kagent variables (currently required)
      - KAGENT_URL=placeholder
      - KAGENT_NAME=placeholder
      - KAGENT_NAMESPACE=placeholder
      - AGENT_NAME=agentdemo
      - OTEL_SERVICE_NAME=agentdemo
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT}
      - MODEL_PROVIDER=gemini
      - MODEL_NAME=gemini-2.5-flash
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - type: bind
        source: ./agentdemo
        target: /config

